spring.application.name=family-tree
#db (docker for me)

# with this environment variables will work docker
#spring.datasource.url=${SPRING_DATASOURCE_URL}
#spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
#spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
#spring.datasource.driver-class-name=org.postgresql.Driver

# with this properties - maven install will work
spring.datasource.url=jdbc:postgresql://localhost:5432/family-tree
spring.datasource.username=postgres
spring.datasource.password=1234
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.defer-datasource-initialization=true
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Mail settings (example for --> Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=vitathetree@gmail.com
spring.mail.password=okskdyknykbejbsd
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

server.port=${SPRING_PORT:8080}

# JWT settings (change secret in production)
jwt.secret=very_secret_key_which_should_be_long_and_kept_safe
jwt.expiration-ms=3600000   # 1 ??????

# ?????
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=uploads/photos
app.upload.max-file-size=10485760

# ???????? ???????
spring.web.resources.static-locations=file:uploads/
